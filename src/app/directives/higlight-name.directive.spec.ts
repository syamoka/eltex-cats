import { HighlightNameDirective } from './higlight-name.directive';

describe('ChangeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightNameDirective();
    expect(directive).toBeTruthy();
  });
});
